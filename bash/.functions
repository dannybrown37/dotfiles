#!/bin/bash

# Create a new directory and enter it
mk() {
    mkdir -p "$@" && cd "$@"
}

# Used to show git branch in command prompt
parse_git_branch() {
    git branch 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/\1/'
}
