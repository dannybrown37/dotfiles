#!/bin/bash


# source all scripts in the path_scripts directory
# they are in the PATH but need to be sourced to pick up script updates


parent_dir=$(dirname $(dirname $(readlink -f "${BASH_SOURCE}")))
source $parent_dir/path_scripts/source_all.sh
